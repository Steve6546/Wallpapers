This is a Python repo for azm-ai-resolver, a library that attempts to resolve github issues with the AI agent AZM AI.

- Setup: `poetry install --with test --with dev`
- Testing: `poetry run pytest tests/test_*.py`
